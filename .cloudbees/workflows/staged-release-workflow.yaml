apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: staged release workflow
on:
  workflow_dispatch:
    inputs:
      manifest:
        type: string
        required: true
        description: This is a system-generated parameter and is required for use in application releases. Refer to https://docs.cloudbees.com/docs/cloudbees-platform/latest/applications/releases#manifest for the expected format
metadata:
  stages/v1alpha1:
    - name: pre-prod
      jobs:
        - pre-prod
        - EM_approval
    - name: prod
      jobs:
        - prod
jobs:
  pre-prod:
    environment: pre-prod
    uses: ./.cloudbees/workflows/deployer.yaml
    with:
      manifest: ${{ inputs.manifest }}
      environment: ${{ job.environment }}
  prod:
    needs: EM_approval
    environment: prod
    uses: ./.cloudbees/workflows/deployer.yaml
    with:
      manifest: ${{ inputs.manifest }}
      environment: ${{ job.environment }}
  EM_approval:
    needs:
      - pre-prod
    timeout-minutes: 4320
    delegates: cloudbees-io/manual-approval/custom-job.yml@v1
    with:
      instructions: |-
        Hey Aruna, can you please take a look at this before moving to prod? 
        Thanks!
      disallowLaunchByUser: false
      notifyAllEligibleUsers: false
      approvers: ed0b49be-49a6-11ef-94a3-42010a83ae06
